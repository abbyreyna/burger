{{!-- Setup the index.handlebars to have the template that Handlebars can render onto.
Create a button in index.handlebars that will submit the user input into the database. --}}

<div class="row">
    <div class="col-8">


        <h1 class="text-center mb-3">Eat-Da-Burger</h1>

        <h4>Burgers</h4>
        <ul class="list-group">
            {{#each burgers}}
            <li class="list-group-item burger">{{this.burgers}}</li>
            {{/each}}
        </ul>

        <form action="/burgers" method="POST" class="mb-4">
            <div class="form-group">
                <label for="name" class="name">Burger Name</label>
                <input type="text" name="name" class="form-control">
            </div>
            <input type="submit" value="Add Burger" class="btn-primary btn-block addBurger">
        </form>

        <a href="/api/burgers" class="btn btn-light mt-4">Visit API</a>
    </div>
</div>
</div>